/// Internal placeholder types for Bilibili API responses.
///
/// These models are intentionally kept inside the Bilibili package. The public
/// provider API only exposes provider-neutral DTOs.
class BilibiliVideoInfo {
  const BilibiliVideoInfo();
}

class BilibiliPlaybackResponse {
  const BilibiliPlaybackResponse();
}
