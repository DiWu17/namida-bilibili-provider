name: Online Integration

on:
  workflow_dispatch:
  schedule:
    - cron: '23 5 * * 1'

jobs:
  online:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/bilibili_provider
    steps:
      - uses: actions/checkout@v4
      - uses: dart-lang/setup-dart@v1
        with:
          sdk: stable
      - run: dart pub get
      - run: dart test --tags online
